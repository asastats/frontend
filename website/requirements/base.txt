#git+https://github.com/algorand/py-algorand-sdk.git
py-algorand-sdk>=2.12.0
Django>=5.2.15,<6
django-htmx>=1.27.0
django-template-partials>=25.3
django-cors-headers>=4.9.0
channels[daphne]>=4.3.2
channels-redis>=4.3.0
requests>=2.34.2
python-dateutil>=2.9
#### database
psycopg[binary]>=3.3.4
#### ANS
pyteal>=0.27.0
## minify
django-minify-html>=1.14.0
## auth
django-allauth[socialaccount]>=65.18.0
django-simple-captcha>=0.6.3
python-dotenv>=1.2.2
hashids>=1.3.1
eth-account>=0.13.7
## API
djangorestframework>=3.17.1
djangorestframework-simplejwt>=5.5.1
drf-spectacular>=0.29.0  # TODO: pin an exact/latest version in production
drf-spectacular-sidecar>=2026.6.1
djangorestframework-xml>=2.0.0
djangorestframework-yaml>=2.0.0

## Web push, for the alerts widget
# pywebpush brings py_vapid, which is also what generates the key pair - see
# post-deploy/RUN-vapid-keys.md.
#
# **No task queue here, and that was checked twice.** The engine runs huey
# already - `engine/settings.py:54`, and `core/views.py:551` enqueues the
# historic widget's price and ledger processing on it - so the background half
# of alerts belongs there, beside work of exactly that shape. This project's
# part is delivery: look up a subscription and send one request.
pywebpush>=2.0.0
